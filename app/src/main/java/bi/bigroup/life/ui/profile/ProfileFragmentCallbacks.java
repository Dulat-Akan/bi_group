package bi.bigroup.life.ui.profile;

import android.widget.ImageView;

public interface ProfileFragmentCallbacks {
    void onFullImageView(ImageView img_avatar);

    void showFullAvatar(String profilePicture);
}
